Use `architect-agent` to read `AGENTS.md` and `docs/**`, then create a phased issue backlog for the requested scope.

Every issue must include:
- owner agent;
- scope;
- out of scope;
- acceptance criteria;
- tests required;
- dependencies;
- likely affected files.
