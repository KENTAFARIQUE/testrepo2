---
description: Implement a single issue with required tests
agent: build
---

Implement this issue only. Follow `AGENTS.md`, `docs/testing/strategy.md`, and `docs/issues/issue-driven-rules.md`.

Requirements:
- Keep scope tight.
- Add or update tests.
- Use fake dependencies in unit tests.
- Run focused tests.
- Report changed files and commands run.

Issue:

$ARGUMENTS
